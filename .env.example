# Non-sensitive runtime configuration
# Copy to .env and adjust as needed:
#   cp .env.example .env

# Database name (secrets db_user and db_password are managed via backend/secrets/)
POSTGRES_DB=srm_credit_engine
POSTGRES_PORT=5432

# Backend Spring Boot port
BACKEND_PORT=8080

# Frontend Nginx port
FRONTEND_PORT=4200
